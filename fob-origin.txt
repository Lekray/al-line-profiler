# Происхождение LineProfiler.fob — с какого текста он снят.
#
# txt  — SHA-256 файла LineProfiler.txt на момент выгрузки .fob из C/SIDE.
# fob  — SHA-256 самого LineProfiler.fob.
#
# Если хэш текста разошёлся с текущим LineProfiler.txt, значит текст правили после
# выгрузки и .fob отстал: импортировать его — поставить старый код. Лечится только
# перевыгрузкой с базы, в которую импортирован текущий текст; собрать .fob из .txt
# скриптом нельзя, это делает C/SIDE.
#
# Оглавление .fob сверяется отдельно (состав объектов, имена, даты, списки версий),
# но доказать происхождение оно не может: дата в OBJECT-PROPERTIES ставится руками,
# и правка кода в тот же день оглавление не меняет вовсе.
#
# Обновляется скриптом scripts/Update-FobOrigin.ps1 СРАЗУ ПОСЛЕ перевыгрузки .fob,
# и только тогда. Запуск «чтобы проверка позеленела» превращает её в ложь.

txt      eefbbadbab3e0f24bab01ca5a9bb7cef656bc32c4b3acfc3c13fe6da2297351a
fob      1e78b9fc77ec815d2de834468fed84cfee31de6aa93ed9df3d31bfedc3135725
platform 11.0.36462.0
